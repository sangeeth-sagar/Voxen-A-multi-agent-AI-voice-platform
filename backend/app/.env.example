# Existing environment variables (if any) should be kept.
# Add the new ones as per instructions:

JWT_SECRET=change-me-to-a-random-32-character-string-in-production
JWT_EXPIRE_MINUTES=1440
MAX_AGENTS_PER_USER=20
MAX_FILE_UPLOAD_MB=10